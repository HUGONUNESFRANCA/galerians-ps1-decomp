update port/src/main.cpp to load a real TIM from 
port/assets/textures_real/BGTIM_B/ instead of port/assets/model/

The TIM files there are 8bpp with CLUT. Make sure tim_loader.cpp
handles 8bpp correctly:

8bpp TIM decoding:
- Each byte in pixel_data is an index into the CLUT (0-255)
- CLUT has 256 entries of RGBA5551 (2 bytes each)
- Output RGBA8 by looking up each byte in the CLUT table
- actual_pixel_width = width_units * 2  (for 8bpp)

Update main.cpp:
- Search in port/assets/textures_real/BGTIM_B/ for first .tim
- Load it with TIM_Load
- Display as textured quad filling most of the screen
- Show filename + dimensions in title bar

Also update CMakeLists.txt if needed so assets path works
relative to the executable.

Rebuild and run.