/*
 * Assets — TIM Loader
 *
 * Will decode PS1 TIM textures (4/8/16 bpp + CLUT) and upload them
 * as GL_TEXTURE_2D objects. TIM magic is 0x10 (see docs/asset_format.md
 * → "Sub-file discovery — magic-byte scan").
 *
 * Empty translation unit until the upload path is implemented.
 */
